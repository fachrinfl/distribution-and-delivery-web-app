# Mapbox Public Access Token
# IMPORTANT: Use a PUBLIC token (pk.*) not a SECRET token (sk.*)
# Get your public token from: https://account.mapbox.com/access-tokens/
# Secret tokens (sk.*) are for server-side use only and will cause errors in Mapbox GL
# 
# Copy this file to .env.local and replace the token with your actual public token
NEXT_PUBLIC_MAPBOX_TOKEN=pk.your_public_token_here

# Supabase Configuration
# Get these from your Supabase project settings: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Supabase Service Role Key (for seeding only - keep this secret!)
# Only use this in seed scripts, never expose in client-side code
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

